import blueprintController from './blueprint.controller';
import blueprintBody from './blueprint.template.html';
import './blueprint.style.scss';

const blueprintcomponent = {
    template: blueprintBody,
    controller: blueprintController,
};

export default blueprintcomponent;
